<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Post-splash theme. Compose paints everything; this just provides the window background. -->
    <style name="Theme.BPlayer" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash theme used as the launch theme. AndroidX SplashScreen chains into Theme.BPlayer
         once the Activity calls installSplashScreen(). -->
    <style name="Theme.BPlayer.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.BPlayer</item>
    </style>
</resources>
